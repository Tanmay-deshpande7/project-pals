const App = () => {
    return (
        <React.Fragment>
            <window.AdminAuth />
        </React.Fragment>
    );
};

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);
